[package]
name = "fortress"
version = "0.1.0"
edition = "2021"

[dependencies]
# Cryptography & Blockchain
bip39         = { version = "2.1", features = ["all-languages"] }
bitcoin       = "0.32"
bs58          = "0.5"
ed25519-dalek = "2"
hex           = "0.4"
hmac          = "0.12"
sskr          = "0.8"
sha2          = "0.10"
tempfile      = "3"
tiny-keccak   = { version = "2.0", features = ["keccak"] }
zeroize       = "1"

# Utilities
rand          = { version = "0.8", features = ["std"] }
dirs          = "5"
flate2        = "1"
serde         = { version = "1.0", features = ["derive"] }
serde_json    = "1.0"
tar           = "0.4"
ureq          = { version = "2.12", features = ["json"] }
zip           = { version = "2", default-features = false, features = ["deflate"] }
eframe        = { version = "0.34.2", default-features = false, features = ["accesskit", "default_fonts", "wayland", "wgpu", "x11"] }
rfd           = "0.17.2"

[target.'cfg(windows)'.build-dependencies]
winresource = "0.1"

[dev-dependencies]
skrifa = "0.40"

[profile.release]
debug = false
panic = "abort"
strip = true
lto = true
codegen-units = 1
